// EmojiLang example file

// Dataflow definition (reversed assignment)
42 => answer
"hello" => greeting

// Conditional expression
x > 0 ?? "positive" :: "non-positive"

// Array literal with slicing
#[1, 2, 3, 4, 5]
arr[1:3]
arr[::2]

// Map literal
#{name: "Alice", age: 30}

// Strategy annotations
~~ expensive_computation
$$ greedy_search

// Iteration/mapping
@@ items >> transform

// Postfix chains
obj.method(arg)[0]

// Nested expressions
(a + b) * c

// Binary operations
1 + 2 * 3
